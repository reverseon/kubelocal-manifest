apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: workflows-sandbox
resources:
- namespace.yaml
- rbac.yaml
- build-workflow.yaml
- reset-registry-workflow.yaml

configMapGenerator:
- name: dockerfile
  files:
  - files/Dockerfile
  options:
    disableNameSuffixHash: true