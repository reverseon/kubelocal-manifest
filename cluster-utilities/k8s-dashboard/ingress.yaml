apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubernetes-dashboard
  namespace: k8s-dashboard
  annotations:
    cert-manager.io/cluster-issuer: "ishiori-local-general-issuer"
    cert-manager.io/common-name: "dash.kube.ishiori.local"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/proxy-set-headers: "dash-proxy-headers"
spec:
  ingressClassName: nginx
  rules:
    - host: dash.kube.ishiori.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kubernetes-dashboard-kong-proxy
                port:
                  number: 443
  tls:
    - hosts:
        - dash.kube.ishiori.local
      secretName: kubernetes-dashboard-tls


